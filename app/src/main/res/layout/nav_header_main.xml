<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="#eeeeee"
    android:gravity="bottom"
    android:orientation="vertical"
    >
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile_imageView"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/ic_profile"
        android:layout_centerInParent="true"
        android:layout_marginTop="15dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/profile_imageView"
        android:layout_centerHorizontal="true"
        android:id="@+id/userNameNavigationBar"
        android:text="Name of the user"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        android:textSize="24sp"
        />
<ImageView
    android:layout_width="wrap_content"
    android:layout_height="20sp"
    android:layout_below="@+id/userNameNavigationBar"
    android:layout_toLeftOf="@+id/userAddressNavigationBar"
    android:layout_marginTop="5dp"
    android:src="@drawable/ic_current_location"


    />
    <TextView
        android:id="@+id/userAddressNavigationBar"
        android:layout_width="wrap_content"
        android:layout_height="20sp"
        android:text="any extra detail"
        android:layout_below="@+id/userNameNavigationBar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="5dp"/>

    </RelativeLayout>
</LinearLayout>
